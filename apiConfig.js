// apiConfig.js - Create this file in your project
// This makes it easy to switch between development and production APIs later

const config = {
    // Development API (JSON Server)
    apiBaseUrl: 'http://localhost:3000'
    
    // Production API - uncomment this when your real backend is ready
    // apiBaseUrl: 'https://your-production-api.com/api'
  };
  
  export default config;